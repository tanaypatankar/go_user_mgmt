# Go User Management

A RESTful API written in Go

## Installation

Start the database with

```bash
docker-compose up -d
```
Run main.go
```bash
go run main.go
```
Once done, remove the docker image
```bash
docker-compose down
```
